---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/integration.rs
  - mnemonic.toml.example
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "Integration test proves memories table exists with all 8 columns after db::open()"
    - "Integration test proves WAL mode is active — PRAGMA journal_mode returns 'wal'"
    - "Integration test proves vec_memories virtual table exists"
    - "Integration test proves embedding_model column is present in memories table"
    - "Integration test proves db::open() works inside a #[tokio::test] (async, non-blocking)"
    - "Example TOML file documents all config fields with snake_case keys"
    - "Full cargo test suite passes with all unit and integration tests green"
  artifacts:
    - path: "tests/integration.rs"
      provides: "Integration tests for database schema, WAL, and sqlite-vec"
      contains: "#[tokio::test]"
      min_lines: 50
    - path: "mnemonic.toml.example"
      provides: "Example configuration file showing all settings"
      contains: "port"
  key_links:
    - from: "tests/integration.rs"
      to: "src/db.rs"
      via: "calls register_sqlite_vec() then open() with test config"
      pattern: "db::register_sqlite_vec.*db::open"
    - from: "tests/integration.rs"
      to: "src/config.rs"
      via: "constructs Config with test values"
      pattern: "Config"
---

<objective>
Create integration tests that verify the database schema, WAL mode, sqlite-vec, and async operation, plus a mnemonic.toml.example file documenting all configuration options.

Purpose: Provides automated proof that all Phase 1 requirements are met. Tests run against an in-memory SQLite database to avoid file system side effects. The example TOML file serves as documentation for users configuring mnemonic.

Output: tests/integration.rs with tests for all STOR-* requirements, mnemonic.toml.example with documented config fields.
</objective>

<execution_context>
@/Users/chrisesposito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisesposito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Types and functions the tests need to call -->

From src/config.rs:
```rust
pub struct Config {
    pub port: u16,
    pub db_path: String,
    pub embedding_provider: String,
}
impl Default for Config { ... }
```

From src/db.rs:
```rust
pub fn register_sqlite_vec();
pub async fn open(config: &Config) -> Result<tokio_rusqlite::Connection, DbError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write integration tests for database requirements</name>
  <files>tests/integration.rs</files>
  <read_first>
    - src/db.rs (register_sqlite_vec and open functions to test)
    - src/config.rs (Config struct to construct test configs)
    - src/error.rs (error types)
    - .planning/phases/01-foundation/01-RESEARCH.md (test map and validation architecture section)
  </read_first>
  <action>
Create `tests/integration.rs` with these integration tests:

1. **Shared setup** at the top of the file:
   - `use std::sync::Once;`
   - `static INIT: Once = Once::new();`
   - Helper function `fn setup() { INIT.call_once(|| { mnemonic::db::register_sqlite_vec(); }); }`
   - Helper function to create a test config: `fn test_config() -> mnemonic::config::Config { mnemonic::config::Config { port: 0, db_path: ":memory:".to_string(), embedding_provider: "local".to_string() } }`
   - Call `setup()` at the start of every test function

   IMPORTANT: For tests to import from the mnemonic crate, the crate must be imported via `use mnemonic;` or by module path. Integration tests in `tests/` automatically have access to the crate as an external dependency. If `src/lib.rs` doesn't exist, the test must reference the binary crate. Create a minimal `src/lib.rs` that re-exports the modules:
   ```rust
   pub mod config;
   pub mod db;
   pub mod error;
   pub mod server;
   ```
   Add this as a first step if needed to make integration tests work.

2. **test_schema_created** (`#[tokio::test]`):
   - Call `setup()`, create test_config with db_path ":memory:"
   - Call `mnemonic::db::open(&config).await.unwrap()`
   - Use `conn.call(|c| { ... }).await` to query `SELECT name FROM sqlite_master WHERE type='table' AND name='memories'`
   - Assert the query returns exactly one row (table exists)
   - Query column info: `PRAGMA table_info(memories)` and collect column names into a Vec<String>
   - Assert the vec contains all 8 column names: "id", "content", "agent_id", "session_id", "tags", "embedding_model", "created_at", "updated_at"

3. **test_wal_mode** (`#[tokio::test]`):
   - Call `setup()`, open in-memory db
   - Use `conn.call(|c| { ... }).await` to query `PRAGMA journal_mode`
   - Assert result is "wal"

4. **test_vec_memories_exists** (`#[tokio::test]`):
   - Call `setup()`, open in-memory db
   - Use `conn.call(|c| { ... }).await` to query `SELECT name FROM sqlite_master WHERE type='table' AND name='vec_memories'`
   - Assert the query returns exactly one row

5. **test_embedding_model_column** (`#[tokio::test]`):
   - Call `setup()`, open in-memory db
   - Use `conn.call(|c| { ... }).await` to query `PRAGMA table_info(memories)` and find a row where the name column is "embedding_model"
   - Assert it exists and its type is "TEXT"

6. **test_db_open_async** (`#[tokio::test]`):
   - Call `setup()`, open in-memory db
   - This test succeeding proves the operation runs in an async context without blocking
   - Insert a test row via conn.call: `INSERT INTO memories (id, content) VALUES ('test-id', 'test content')`
   - Query it back: `SELECT content FROM memories WHERE id = 'test-id'`
   - Assert content is "test content"

All tests use `:memory:` as db_path to avoid file system side effects. All SQL queries go through `conn.call(|c| { ... }).await` — never direct rusqlite calls.
  </action>
  <verify>
    <automated>cd /Users/chrisesposito/Documents/github/mnemonic && cargo test -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration.rs contains `#[tokio::test]` (at least 5 instances)
    - tests/integration.rs contains `fn setup()`
    - tests/integration.rs contains `register_sqlite_vec`
    - tests/integration.rs contains `test_schema_created`
    - tests/integration.rs contains `test_wal_mode`
    - tests/integration.rs contains `test_vec_memories_exists`
    - tests/integration.rs contains `test_embedding_model_column`
    - tests/integration.rs contains `test_db_open_async`
    - tests/integration.rs contains `":memory:"` (in-memory DB for tests)
    - `cargo test -- --test-threads=1` exits 0 with all tests passing
    - If src/lib.rs was created, it contains `pub mod config` and `pub mod db`
  </acceptance_criteria>
  <done>Integration tests verify all STOR requirements: memories table exists with 8 columns (STOR-01), WAL mode is active (STOR-02), vec_memories virtual table exists (STOR-01), embedding_model column is TEXT (STOR-04), async open + insert/query works via tokio-rusqlite (STOR-03). All tests pass with --test-threads=1.</done>
</task>

<task type="auto">
  <name>Task 2: Create example TOML config file</name>
  <files>mnemonic.toml.example</files>
  <read_first>
    - src/config.rs (Config struct fields and defaults)
    - .planning/phases/01-foundation/01-CONTEXT.md (config behavior decisions)
  </read_first>
  <action>
Create `mnemonic.toml.example` documenting all available configuration options with comments explaining each field, its default, and the corresponding environment variable:

```toml
# Mnemonic Configuration
# Copy to mnemonic.toml and modify as needed.
# Environment variables override these values (prefix: MNEMONIC_).

# HTTP server port (env: MNEMONIC_PORT, default: 8080)
port = 8080

# Path to the SQLite database file (env: MNEMONIC_DB_PATH, default: "./mnemonic.db")
db_path = "./mnemonic.db"

# Embedding provider: "local" or "openai" (env: MNEMONIC_EMBEDDING_PROVIDER, default: "local")
# When set to "openai", the OPENAI_API_KEY environment variable must also be set.
embedding_provider = "local"
```

Use snake_case for all keys (matching the Config struct field names). Include the MNEMONIC_ prefix env var name as a comment for each field.
  </action>
  <verify>
    <automated>cd /Users/chrisesposito/Documents/github/mnemonic && test -f mnemonic.toml.example && grep -c "port\|db_path\|embedding_provider" mnemonic.toml.example</automated>
  </verify>
  <acceptance_criteria>
    - mnemonic.toml.example contains `port = 8080`
    - mnemonic.toml.example contains `db_path = "./mnemonic.db"`
    - mnemonic.toml.example contains `embedding_provider = "local"`
    - mnemonic.toml.example contains `MNEMONIC_PORT`
    - mnemonic.toml.example contains `MNEMONIC_DB_PATH`
    - mnemonic.toml.example contains `MNEMONIC_EMBEDDING_PROVIDER`
    - All keys use snake_case (no kebab-case)
  </acceptance_criteria>
  <done>mnemonic.toml.example documents all 3 config fields with defaults, env var names, and descriptive comments. Keys use snake_case matching Config struct.</done>
</task>

</tasks>

<verification>
- `cargo test -- --test-threads=1` runs all tests green (unit tests from Plan 01 + integration tests from this plan)
- All 7 phase requirements have at least one test covering them:
  - STOR-01: test_schema_created, test_vec_memories_exists
  - STOR-02: test_wal_mode
  - STOR-03: test_db_open_async
  - STOR-04: test_embedding_model_column
  - CONF-01: test_config_defaults (from Plan 01)
  - CONF-02: test_config_env_override (from Plan 01)
  - CONF-03: test_config_toml_override (from Plan 01)
- mnemonic.toml.example exists with all config fields documented
</verification>

<success_criteria>
1. `cargo test -- --test-threads=1` passes all tests (config unit tests + integration tests)
2. Every STOR-* requirement verified by at least one integration test
3. mnemonic.toml.example documents all configuration options with env var mappings
4. Phase 1 is feature-complete: compilable binary with SQLite+sqlite-vec, WAL, layered config, health endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
