---
phase: 26-proto-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements:
  - PROTO-03

must_haves:
  truths:
    - "CI release workflow has protoc installed before cargo build step for all three matrix targets"
    - "protoc installation uses arduino/setup-protoc@v3 with repo-token for rate limiting"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "protoc installation step in CI pipeline"
      contains: "arduino/setup-protoc@v3"
  key_links:
    - from: ".github/workflows/release.yml Install protoc step"
      to: ".github/workflows/release.yml Build binary step"
      via: "step ordering — protoc MUST precede cargo build"
      pattern: "Install protoc.*Build binary"
---

<objective>
Add protoc installation to the CI release workflow so that gRPC proto codegen works in CI when the interface-grpc feature is enabled.

Purpose: Without protoc in CI, builds with `--features interface-grpc` fail with cryptic missing-file errors (not a clear "protoc not found"). This must be in place before any CI build enables the feature. Per D-13, this step is added proactively.
Output: Updated .github/workflows/release.yml with arduino/setup-protoc@v3 step
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.5-phases/26-proto-foundation/26-CONTEXT.md
@.planning/milestones/v1.5-phases/26-proto-foundation/26-RESEARCH.md

@.github/workflows/release.yml

<interfaces>
<!-- Current release.yml structure — protoc step inserts between "Install Rust toolchain" and "Build binary" -->

From .github/workflows/release.yml lines 28-38:
```yaml
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build binary
        run: cargo build --release --target ${{ matrix.target }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protoc installation step to release.yml before cargo build</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (current workflow structure — understand exact step ordering and indentation)
    - .planning/milestones/v1.5-phases/26-proto-foundation/26-RESEARCH.md (Pattern 4: CI protoc Installation — exact YAML snippet, Pitfall 3: cryptic error details)
    - .planning/milestones/v1.5-phases/26-proto-foundation/26-CONTEXT.md (D-13: arduino/setup-protoc@v3 in release.yml, D-14: protoc step for all three matrix targets)
  </read_first>
  <action>
    Add a protoc installation step to `.github/workflows/release.yml` in the `build` job. Per D-13 and D-14:

    Insert the following step AFTER "Install Rust toolchain" and BEFORE "Build binary":

    ```yaml
      - name: Install protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
    ```

    The step must be:
    1. Positioned BEFORE `cargo build` — protoc must be on PATH when tonic-build runs during compilation
    2. Applied to ALL three matrix targets (linux-x86_64, macos-x86_64, macos-aarch64) — since it's in the shared `steps` section (not conditional), this is automatic
    3. Using `repo-token: ${{ secrets.GITHUB_TOKEN }}` to avoid GitHub API rate limiting when downloading protoc release binaries

    The resulting steps section should be (showing the full flow):
    ```yaml
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build binary
        run: cargo build --release --target ${{ matrix.target }}
    ```

    Note: The current release build does NOT use `--features interface-grpc`, so protoc won't actually be invoked yet. This is a forward-compatible step — it ensures CI doesn't break when the feature is later enabled in the build command. Per Research open question #2, adding protoc unconditionally is free in CI time cost and prevents future breakage.

    Do NOT modify any other steps or the matrix configuration. Do NOT add `--features interface-grpc` to the build command — that is a future phase concern.
  </action>
  <verify>
    <automated>grep -A2 "Install protoc" .github/workflows/release.yml && grep -c "arduino/setup-protoc@v3" .github/workflows/release.yml && grep -c "repo-token" .github/workflows/release.yml && echo "CI_PROTOC_OK"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release.yml contains step named "Install protoc"
    - Step uses `arduino/setup-protoc@v3` (not v1, not v2, not apt-get)
    - Step has `repo-token: ${{ secrets.GITHUB_TOKEN }}`
    - "Install protoc" step appears AFTER "Install Rust toolchain" and BEFORE "Build binary" in the YAML
    - No other steps are modified or removed
    - Matrix configuration is unchanged (still includes linux-x86_64, macos-x86_64, macos-aarch64)
    - Build command is still `cargo build --release --target ${{ matrix.target }}` (no features flag added)
  </acceptance_criteria>
  <done>release.yml has arduino/setup-protoc@v3 step installed before cargo build for all three matrix targets, with repo-token for rate limiting</done>
</task>

</tasks>

<verification>
1. `grep -n "Install protoc" .github/workflows/release.yml` — returns a line number
2. `grep -n "arduino/setup-protoc@v3" .github/workflows/release.yml` — returns a line number
3. `grep -n "repo-token" .github/workflows/release.yml` — returns a line number
4. Verify ordering: "Install protoc" line number is LESS than "Build binary" line number and GREATER than "Install Rust toolchain" line number
5. `grep -c "matrix:" .github/workflows/release.yml` — still 1 (matrix config unchanged)
</verification>

<success_criteria>
- arduino/setup-protoc@v3 step is in release.yml between Rust toolchain install and cargo build
- repo-token configured to avoid rate limiting
- All three matrix targets covered (step is in shared steps section)
- No other workflow changes introduced
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.5-phases/26-proto-foundation/26-02-SUMMARY.md`
</output>
