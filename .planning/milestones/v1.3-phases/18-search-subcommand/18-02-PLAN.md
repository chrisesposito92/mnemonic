---
phase: 18-search-subcommand
plan: "02"
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - tests/cli_integration.rs
autonomous: true
requirements: [SRC-01, SRC-02]
must_haves:
  truths:
    - "mnemonic search 'query' returns ranked results with DIST column and footer"
    - "mnemonic search 'query' --limit 2 caps results at 2"
    - "mnemonic search 'query' --threshold 0.0001 filters out non-exact matches"
    - "mnemonic search 'query' --agent-id x only returns memories for that agent"
    - "mnemonic search '' exits 1 with 'query must not be empty' on stderr"
    - "mnemonic --help shows 'search' in subcommands list"
    - "mnemonic search 'nonexistent' prints 'No matching memories found.'"
  artifacts:
    - path: "tests/cli_integration.rs"
      provides: "Phase 18 search integration tests"
      contains: "Phase 18: search subcommand"
  key_links:
    - from: "tests/cli_integration.rs"
      to: "mnemonic binary"
      via: "std::process::Command invocation"
      pattern: "\"search\""
---

<objective>
Add integration tests for `mnemonic search` subcommand covering end-to-end semantic search, filter flags, error paths, and help text.

Purpose: Validate SRC-01 (semantic search with results) and SRC-02 (limit/threshold/agent-id/session-id flags) via black-box binary invocation tests. Mirrors the Phase 17 test plan structure.

Output: 7-8 integration tests appended to tests/cli_integration.rs in a new Phase 18 section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-search-subcommand/18-CONTEXT.md
@.planning/phases/18-search-subcommand/18-RESEARCH.md
@.planning/phases/18-search-subcommand/18-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From tests/cli_integration.rs (test infrastructure, already exists):
```rust
fn binary() -> PathBuf { ... }  // Returns path to compiled mnemonic binary

struct TempDb { pub path: PathBuf }
impl TempDb {
    fn new(label: &str) -> Self { ... }  // Creates temp DB file
    fn path_str(&self) -> &str { ... }   // Returns path as &str
}
impl Drop for TempDb { ... }  // Cleans up DB + WAL/SHM files
```

From src/cli.rs (after Plan 01 -- SearchArgs struct):
```rust
pub struct SearchArgs {
    pub query: String,
    pub agent_id: Option<String>,
    pub session_id: Option<String>,
    pub limit: u32,        // default: 10
    pub threshold: Option<f32>,
}
```

Expected CLI behavior (from CONTEXT.md decisions):
- `mnemonic search "query"` -- performs semantic search, prints table with DIST/ID/CONTENT/AGENT columns + footer
- `mnemonic search ""` -- exits 1 with "error: query must not be empty" on stderr (fast, no model load)
- `mnemonic search "query" --limit 5` -- caps results at 5
- `mnemonic search "query" --threshold 0.8` -- filters by max distance
- `mnemonic search "query" --agent-id x` -- filters by agent
- `mnemonic search "query" --session-id y` -- filters by session
- Empty results: "No matching memories found." on stdout
- Footer: "Found N results" (or "Found 1 result" for singular)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search integration tests for core functionality, empty results, and error paths</name>
  <files>tests/cli_integration.rs</files>
  <read_first>
    - tests/cli_integration.rs (full file -- understand binary(), TempDb pattern, Phase 17 test section structure at end of file)
    - src/cli.rs (confirm SearchArgs, run_search, and table format after Plan 01)
    - src/main.rs (confirm Search match arm after Plan 01)
  </read_first>
  <action>
Append a new Phase 18 section to the end of tests/cli_integration.rs. Add these 5 tests:

**Section header comment:**
```rust
// ---- Phase 18: search subcommand ------------------------------------------------
```

**Test 1: test_search_returns_ranked_results (SRC-01 end-to-end)**
- Create `TempDb::new("search_basic")`
- Seed a memory: `Command::new(&bin).args(["--db", db.path_str(), "remember", "Paris is the capital of France"]).output()` -- assert success
- Search for it: `Command::new(&bin).args(["--db", db.path_str(), "search", "French capital city"]).output()`
- Assert exit code 0
- Assert stdout contains `"DIST"` (column header)
- Assert stdout contains `"ID"` (column header)
- Assert stdout contains `"CONTENT"` (column header)
- Assert stdout contains `"AGENT"` (column header)
- Assert stdout contains `"Found"` (footer line)
- Assert stdout contains `"Paris"` (content should appear in truncated form)
- Note: This test takes ~4-6s (model load for remember + model load for search)

**Test 2: test_search_empty_query_exits_one (SRC-01 error path)**
- Create `TempDb::new("search_empty_query")`
- Run: `Command::new(&bin).args(["--db", db.path_str(), "search", ""]).output()`
- Assert exit code is NOT success (non-zero)
- Assert stderr contains `"query must not be empty"` (per D-05)
- This test is FAST (no model load due to early validation per D-04)

**Test 3: test_search_whitespace_query_exits_one (SRC-01 error path)**
- Create `TempDb::new("search_whitespace_query")`
- Run: `Command::new(&bin).args(["--db", db.path_str(), "search", "   "]).output()`
- Assert exit code is NOT success
- Assert stderr contains `"query must not be empty"`
- This test is FAST (no model load)

**Test 4: test_search_no_results_message (SRC-01 empty results)**
- Create `TempDb::new("search_no_results")`
- Do NOT seed any memories
- Run: `Command::new(&bin).args(["--db", db.path_str(), "search", "something that does not exist"]).output()`
- Assert exit code 0 (empty results is success, not error)
- Assert stdout contains `"No matching memories found."` (per D-13)
- Note: This test takes ~2-3s (model load for search even though no results)

**Test 5: test_search_appears_in_help (SRC-01 discoverability)**
- Create `TempDb::new("search_help")`
- Run: `Command::new(&bin).args(["--help"]).output()`
- Assert exit code 0
- Assert stdout contains `"search"` (the subcommand name in help text)
- This test is FAST (no model load)
  </action>
  <verify>
    <automated>cd /Users/chrisesposito/Documents/github/mnemonic && cargo test --test cli_integration test_search -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/cli_integration.rs contains `// ---- Phase 18: search subcommand`
    - tests/cli_integration.rs contains `fn test_search_returns_ranked_results()`
    - tests/cli_integration.rs contains `fn test_search_empty_query_exits_one()`
    - tests/cli_integration.rs contains `fn test_search_whitespace_query_exits_one()`
    - tests/cli_integration.rs contains `fn test_search_no_results_message()`
    - tests/cli_integration.rs contains `fn test_search_appears_in_help()`
    - test_search_returns_ranked_results asserts stdout contains "DIST" and "Found" and "Paris"
    - test_search_empty_query_exits_one asserts stderr contains "query must not be empty"
    - test_search_no_results_message asserts stdout contains "No matching memories found."
    - `cargo test --test cli_integration test_search` exits 0 (all 5 tests pass)
  </acceptance_criteria>
  <done>5 integration tests cover SRC-01 core: end-to-end search with table output, empty query error, whitespace query error, empty results message, and help text discoverability. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add search integration tests for filter flags (limit, threshold, agent-id, session-id)</name>
  <files>tests/cli_integration.rs</files>
  <read_first>
    - tests/cli_integration.rs (after Task 1 -- confirm Phase 18 section exists with 5 tests)
    - src/cli.rs (confirm SearchArgs fields: limit default 10, threshold Option f32, agent_id Option String, session_id Option String)
  </read_first>
  <action>
Append 3 additional tests to the Phase 18 section in tests/cli_integration.rs, after the 5 tests from Task 1:

**Test 6: test_search_limit_flag (SRC-02 --limit)**
- Create `TempDb::new("search_limit")`
- Seed 3 memories with distinct content via remember:
  - `"The Eiffel Tower is in Paris France"`
  - `"The Louvre Museum is in Paris France"`
  - `"Notre Dame Cathedral is in Paris France"`
- Search with limit 2: `Command::new(&bin).args(["--db", db.path_str(), "search", "Paris landmarks", "--limit", "2"]).output()`
- Assert exit code 0
- Assert stdout contains `"Found 2 results"` (exactly 2, not 3)
- Note: This test takes ~10-12s (3 remember + 1 search model loads). Keep it as the ONLY multi-seed test to control suite time.

**Test 7: test_search_agent_id_filter (SRC-02 --agent-id)**
- Create `TempDb::new("search_agent_filter")`
- Seed 1 memory with agent-id: `Command::new(&bin).args(["--db", db.path_str(), "remember", "Tokyo is the capital of Japan", "--agent-id", "agent-jp"]).output()` -- assert success
- Seed 1 memory without agent-id: `Command::new(&bin).args(["--db", db.path_str(), "remember", "Berlin is the capital of Germany"]).output()` -- assert success
- Search with agent-id filter: `Command::new(&bin).args(["--db", db.path_str(), "search", "capital city", "--agent-id", "agent-jp"]).output()`
- Assert exit code 0
- Assert stdout contains `"Found 1 result"` (singular -- only the agent-jp memory)
- Assert stdout contains `"Tokyo"` (the filtered memory content)
- Note: This test takes ~8-10s (2 remember + 1 search model loads)

**Test 8: test_search_threshold_flag (SRC-02 --threshold)**
- Create `TempDb::new("search_threshold")`
- Seed 1 memory: `Command::new(&bin).args(["--db", db.path_str(), "remember", "Machine learning uses neural networks"]).output()` -- assert success
- Search with very tight threshold (should exclude): `Command::new(&bin).args(["--db", db.path_str(), "search", "something completely unrelated to ML", "--threshold", "0.0001"]).output()`
- Assert exit code 0
- Assert stdout contains `"No matching memories found."` (threshold filters out the non-exact match)
- Note: This test takes ~4-6s (1 remember + 1 search model loads). The 0.0001 threshold is tight enough that only near-exact embedding matches pass.
  </action>
  <verify>
    <automated>cd /Users/chrisesposito/Documents/github/mnemonic && cargo test --test cli_integration test_search -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/cli_integration.rs contains `fn test_search_limit_flag()`
    - tests/cli_integration.rs contains `fn test_search_agent_id_filter()`
    - tests/cli_integration.rs contains `fn test_search_threshold_flag()`
    - test_search_limit_flag asserts stdout contains "Found 2 results"
    - test_search_agent_id_filter asserts stdout contains "Found 1 result" and "Tokyo"
    - test_search_threshold_flag asserts stdout contains "No matching memories found."
    - `cargo test --test cli_integration test_search -- --test-threads=1` exits 0 (all 8 tests pass)
    - `cargo test` exits 0 (full suite green)
  </acceptance_criteria>
  <done>3 integration tests cover SRC-02 filter flags: --limit caps result count, --agent-id scopes to agent, --threshold filters by distance. All 8 Phase 18 tests pass. Full test suite green.</done>
</task>

</tasks>

<verification>
- `cargo test --test cli_integration test_search -- --test-threads=1` passes all 8 Phase 18 tests
- `cargo test` passes full suite (unit + integration)
- Phase 18 test section is clearly delineated in tests/cli_integration.rs
- Tests cover both SRC-01 (search + display) and SRC-02 (all four filter flags)
</verification>

<success_criteria>
- 8 integration tests in Phase 18 section of tests/cli_integration.rs
- SRC-01 covered: end-to-end search, table output format, empty results, error paths, help text
- SRC-02 covered: --limit, --threshold, --agent-id flags verified (--session-id shares the same code path as --agent-id and is covered by --agent-id test pattern)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/18-search-subcommand/18-02-SUMMARY.md`
</output>
